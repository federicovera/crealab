# JWT Auth CreaLab — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Conectar el formulario de login con WordPress via JWT Auth: cookie httpOnly, protección de rutas con middleware, y redirect al curso activo o dashboard post-login.

**Architecture:** La API route `/api/auth/login` actúa de proxy seguro: recibe credenciales del browser, llama a WP, guarda el JWT en cookie httpOnly, detecta el curso activo y devuelve la URL de redirect. El middleware protege `/dashboard` y `/curso/*`. LoginForm llama al proxy y navega al redirect.

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind v4, WordPress REST API, JWT Authentication for WP REST API plugin

---

## File Map

| Archivo | Acción | Responsabilidad |
|---------|--------|-----------------|
| `docker-compose.yml` | Modificar | Agregar JWT_SECRET a WordPress y WP_URL al frontend |
| `.env` (servidor) | Documentar | Variable JWT_SECRET |
| `frontend/lib/auth.ts` | Crear | getTokenFromCookie, verifyToken, getActiveCourse |
| `frontend/app/api/auth/login/route.ts` | Crear | Proxy POST login → WordPress JWT |
| `frontend/middleware.ts` | Crear | Proteger /dashboard y /curso/* |
| `frontend/components/LoginForm.tsx` | Modificar | Conectar a /api/auth/login |
| `frontend/app/dashboard/page.tsx` | Crear | Placeholder dashboard (ruta protegida) |
| `frontend/app/curso/[id]/page.tsx` | Crear | Placeholder curso (ruta protegida) |

---

## Task 1: Configurar WordPress JWT Auth plugin y variables de entorno

**Files:**
- Modify: `docker-compose.yml`

- [ ] **Step 1: Instalar el plugin JWT Authentication for WP REST API en WordPress**

En el panel de WordPress (`https://crealab.flowhub.com.ar/wp-admin`):
- Plugins → Añadir nuevo → buscar "JWT Authentication for WP REST API"
- Autor: Useful Team. Instalar y activar.

- [ ] **Step 2: Agregar JWT_SECRET al .env del servidor**

En el servidor (`/opt/crealab/.env`), agregar:

```
JWT_SECRET=crealab-jwt-secret-2026-muy-largo-y-aleatorio-cambiar-en-produccion
```

Generar un valor aleatorio real:
```bash
openssl rand -hex 48
```

- [ ] **Step 3: Actualizar docker-compose.yml**

Reemplazar el bloque `WORDPRESS_CONFIG_EXTRA` y agregar `WP_URL` al frontend:

```yaml
services:
  crealab_db:
    image: mariadb:11
    container_name: crealab_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: crealab
      MYSQL_USER: crealab_user
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - crealab_db_data:/var/lib/mysql

  crealab_wp:
    image: wordpress:latest
    container_name: crealab_wp
    restart: always
    depends_on:
      - crealab_db
    environment:
      WORDPRESS_DB_HOST: crealab_db:3306
      WORDPRESS_DB_NAME: crealab
      WORDPRESS_DB_USER: crealab_user
      WORDPRESS_DB_PASSWORD: ${DB_PASSWORD}
      WORDPRESS_CONFIG_EXTRA: |
        if (isset($$_SERVER['HTTP_X_FORWARDED_PROTO']) && $$_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
          $$_SERVER['HTTPS'] = 'on';
        }
        define('JWT_AUTH_SECRET_KEY', '${JWT_SECRET}');
        define('JWT_AUTH_CORS_ENABLE', true);
    ports:
      - "127.0.0.1:8081:80"
    volumes:
      - crealab_wp_data:/var/www/html

  crealab_frontend:
    build: ./frontend
    container_name: crealab_frontend
    restart: always
    ports:
      - "127.0.0.1:3002:3000"
    environment:
      NEXT_PUBLIC_WP_URL: https://crealab.flowhub.com.ar
      WP_URL: https://crealab.flowhub.com.ar

volumes:
  crealab_db_data:
  crealab_wp_data:
```

- [ ] **Step 4: Verificar que el endpoint JWT existe**

Antes de seguir, verificar que WP expone el endpoint JWT. En el servidor:

```bash
curl -s https://crealab.flowhub.com.ar/wp-json/jwt-auth/v1/token \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"CONTRASENA_REAL"}' | head -c 200
```

Expected: respuesta JSON con campo `token` o error `[jwt_auth_failed]`. Si devuelve 404, el plugin no está activo.

- [ ] **Step 5: Reiniciar WordPress con nueva config**

En el servidor:

```bash
cd /opt/crealab
docker compose up -d crealab_wp
```

- [ ] **Step 6: Commit**

```bash
cd /Users/federicovera/Documents/Software/crealab
git add docker-compose.yml
git commit -m "feat: agregar JWT_SECRET a WordPress y WP_URL al frontend en docker-compose"
```

---

## Task 2: Crear lib/auth.ts — funciones utilitarias de autenticación

**Files:**
- Create: `frontend/lib/auth.ts`

- [ ] **Step 1: Crear directorio lib si no existe**

```bash
mkdir -p /Users/federicovera/Documents/Software/crealab/frontend/lib
```

- [ ] **Step 2: Crear frontend/lib/auth.ts**

```typescript
import { cookies } from "next/headers";

const WP_URL = process.env.WP_URL || process.env.NEXT_PUBLIC_WP_URL || "";

export function getTokenFromCookie(): string | undefined {
  const cookieStore = cookies();
  return cookieStore.get("crealab_token")?.value;
}

export async function getActiveCourse(token: string): Promise<string | null> {
  try {
    const res = await fetch(`${WP_URL}/wp-json/wp/v2/users/me?context=edit`, {
      headers: { Authorization: `Bearer ${token}` },
      cache: "no-store",
    });
    if (!res.ok) return null;
    const user = await res.json();
    const courseId = user.meta?.tutor_active_course_id;
    return courseId ? String(courseId) : null;
  } catch {
    return null;
  }
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/federicovera/Documents/Software/crealab
git add frontend/lib/auth.ts
git commit -m "feat: crear lib/auth.ts con getTokenFromCookie y getActiveCourse"
```

---

## Task 3: Crear API route /api/auth/login

**Files:**
- Create: `frontend/app/api/auth/login/route.ts`

- [ ] **Step 1: Crear directorios**

```bash
mkdir -p /Users/federicovera/Documents/Software/crealab/frontend/app/api/auth/login
```

- [ ] **Step 2: Crear frontend/app/api/auth/login/route.ts**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { getActiveCourse } from "@/lib/auth";

const WP_URL = process.env.WP_URL || process.env.NEXT_PUBLIC_WP_URL || "";

export async function POST(request: NextRequest) {
  let body: { email?: string; password?: string };
  try {
    body = await request.json();
  } catch {
    return NextResponse.json({ error: "Solicitud inválida" }, { status: 400 });
  }

  const { email, password } = body;
  if (!email || !password) {
    return NextResponse.json({ error: "Completá todos los campos" }, { status: 400 });
  }

  let wpRes: Response;
  try {
    wpRes = await fetch(`${WP_URL}/wp-json/jwt-auth/v1/token`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ username: email, password }),
      cache: "no-store",
    });
  } catch {
    return NextResponse.json(
      { error: "Error de conexión, intentá más tarde" },
      { status: 503 }
    );
  }

  if (!wpRes.ok) {
    return NextResponse.json(
      { error: "Usuario o contraseña incorrectos" },
      { status: 401 }
    );
  }

  const wpData = await wpRes.json();
  const token: string = wpData.token;

  const courseId = await getActiveCourse(token);
  const redirect = courseId ? `/curso/${courseId}` : "/dashboard";

  const response = NextResponse.json({ redirect });
  response.cookies.set("crealab_token", token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === "production",
    sameSite: "lax",
    maxAge: 60 * 60 * 24 * 7,
    path: "/",
  });

  return response;
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/federicovera/Documents/Software/crealab
git add frontend/app/api/auth/login/route.ts
git commit -m "feat: API route /api/auth/login como proxy seguro a WordPress JWT"
```

---

## Task 4: Crear middleware.ts para proteger rutas

**Files:**
- Create: `frontend/middleware.ts`

- [ ] **Step 1: Crear frontend/middleware.ts**

```typescript
import { NextRequest, NextResponse } from "next/server";

const PROTECTED_PATHS = ["/dashboard", "/curso"];

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;
  const isProtected = PROTECTED_PATHS.some((p) => pathname.startsWith(p));

  if (!isProtected) return NextResponse.next();

  const token = request.cookies.get("crealab_token")?.value;
  if (!token) {
    const loginUrl = request.nextUrl.clone();
    loginUrl.pathname = "/";
    loginUrl.searchParams.set("sesion", "expirada");
    return NextResponse.redirect(loginUrl);
  }

  return NextResponse.next();
}

export const config = {
  matcher: ["/dashboard/:path*", "/curso/:path*"],
};
```

- [ ] **Step 2: Commit**

```bash
cd /Users/federicovera/Documents/Software/crealab
git add frontend/middleware.ts
git commit -m "feat: middleware protege /dashboard y /curso con cookie crealab_token"
```

---

## Task 5: Actualizar LoginForm.tsx para llamar a /api/auth/login

**Files:**
- Modify: `frontend/components/LoginForm.tsx`

- [ ] **Step 1: Reemplazar LoginForm.tsx**

```typescript
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Eye, EyeOff, ArrowRight } from "lucide-react";

export default function LoginForm() {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [showPassword, setShowPassword] = useState(false);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");
  const router = useRouter();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError("");

    if (!email || !password) {
      setError("Completá todos los campos.");
      return;
    }

    setLoading(true);
    try {
      const res = await fetch("/api/auth/login", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email, password }),
      });

      const data = await res.json();

      if (!res.ok) {
        setError(data.error || "Error al iniciar sesión");
        return;
      }

      router.push(data.redirect);
    } catch {
      setError("Error de conexión, intentá más tarde.");
    } finally {
      setLoading(false);
    }
  };

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-4 w-full">
      {error && (
        <div className="bg-red-50 border border-red-200 text-red-600 text-sm rounded-xl px-4 py-3 font-semibold">
          {error}
        </div>
      )}

      <div className="flex flex-col gap-1">
        <label className="text-sm font-bold text-crealab-text">Email</label>
        <input
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="tu@email.com"
          className="bg-crealab-bg border-2 border-gray-200 rounded-xl px-4 py-3 text-sm font-semibold outline-none focus:border-brand-orange transition-colors"
          autoComplete="email"
        />
      </div>

      <div className="flex flex-col gap-1">
        <label className="text-sm font-bold text-crealab-text">Contraseña</label>
        <div className="relative">
          <input
            type={showPassword ? "text" : "password"}
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="••••••••"
            className="w-full bg-crealab-bg border-2 border-gray-200 rounded-xl px-4 py-3 pr-12 text-sm font-semibold outline-none focus:border-brand-orange transition-colors"
            autoComplete="current-password"
          />
          <button
            type="button"
            onClick={() => setShowPassword(!showPassword)}
            className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 hover:text-brand-orange transition-colors"
          >
            {showPassword ? <EyeOff size={18} /> : <Eye size={18} />}
          </button>
        </div>
      </div>

      <button
        type="submit"
        disabled={loading}
        className="flex items-center justify-center gap-2 bg-brand-orange text-white font-bold rounded-xl px-6 py-3 hover:bg-orange-600 transition-colors disabled:opacity-60 mt-2"
      >
        {loading ? "Ingresando..." : "Ingresar"}
        {!loading && <ArrowRight size={18} />}
      </button>

      <p className="text-center text-sm text-gray-500 mt-1">
        ¿No tenés cuenta?{" "}
        <a href="/registro" className="text-brand-orange font-bold hover:underline">
          Registrate
        </a>
      </p>
    </form>
  );
}
```

- [ ] **Step 2: Actualizar page.tsx para leer el param sesion=expirada**

Modificar `frontend/app/page.tsx` para mostrar mensaje si `?sesion=expirada`:

```typescript
import { Suspense } from "react";
import LoginForm from "@/components/LoginForm";
import SessionAlert from "@/components/SessionAlert";

export default function Home() {
  return (
    <main className="min-h-screen flex flex-col lg:flex-row">

      {/* Lado izquierdo — Identidad de marca */}
      <div className="lg:w-1/2 flex flex-col items-center justify-center p-12 text-white"
        style={{ background: "linear-gradient(135deg, #FF6B35 0%, #FFD23F 100%)" }}>

        <div className="max-w-sm w-full flex flex-col gap-6">
          <div className="flex flex-col gap-1">
            <h1 className="text-5xl font-black tracking-tight">
              CreaLab
            </h1>
            <p className="text-lg font-semibold opacity-90">
              Programá. Construí. Aprendé.
            </p>
          </div>

          <div className="flex flex-wrap gap-2">
            {["🤖 Robótica", "💻 Programación", "⚡ STEAM", "🏆 Proyectos reales"].map((tag) => (
              <span
                key={tag}
                className="bg-white/20 text-white text-sm font-bold px-3 py-1 rounded-full"
              >
                {tag}
              </span>
            ))}
          </div>

          <p className="text-base font-semibold opacity-90 leading-relaxed">
            La plataforma educativa para aprender programación y robótica
            construyendo proyectos reales. Para jóvenes de 8 a 18 años.
          </p>
        </div>
      </div>

      {/* Lado derecho — Login */}
      <div className="lg:w-1/2 flex flex-col items-center justify-center p-12 bg-white">
        <div className="max-w-sm w-full flex flex-col gap-8">

          <div className="flex flex-col gap-1">
            <h2 className="text-3xl font-black text-crealab-text">
              Bienvenido 👋
            </h2>
            <p className="text-gray-500 font-semibold">
              Ingresá a tu cuenta para continuar
            </p>
          </div>

          <Suspense>
            <SessionAlert />
          </Suspense>

          <LoginForm />

        </div>
      </div>

    </main>
  );
}
```

- [ ] **Step 3: Crear components/SessionAlert.tsx**

```typescript
"use client";

import { useSearchParams } from "next/navigation";

export default function SessionAlert() {
  const params = useSearchParams();
  if (params.get("sesion") !== "expirada") return null;

  return (
    <div className="bg-amber-50 border border-amber-200 text-amber-700 text-sm rounded-xl px-4 py-3 font-semibold">
      Tu sesión expiró. Ingresá de nuevo.
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
cd /Users/federicovera/Documents/Software/crealab
git add frontend/components/LoginForm.tsx frontend/app/page.tsx frontend/components/SessionAlert.tsx
git commit -m "feat: conectar LoginForm a /api/auth/login con redirect post-login"
```

---

## Task 6: Crear páginas placeholder dashboard y curso

**Files:**
- Create: `frontend/app/dashboard/page.tsx`
- Create: `frontend/app/curso/[id]/page.tsx`

- [ ] **Step 1: Crear frontend/app/dashboard/page.tsx**

```typescript
import { getTokenFromCookie } from "@/lib/auth";

export default function DashboardPage() {
  const token = getTokenFromCookie();

  return (
    <main className="min-h-screen bg-crealab-bg flex flex-col items-center justify-center p-8">
      <div className="max-w-xl w-full flex flex-col gap-6">
        <div className="flex flex-col gap-1">
          <h1 className="text-4xl font-black text-crealab-text">
            Mi Dashboard 🚀
          </h1>
          <p className="text-gray-500 font-semibold">
            Tus cursos y progreso aparecerán aquí.
          </p>
        </div>

        <div className="bg-white rounded-2xl p-6 shadow-sm border border-gray-100 flex flex-col gap-2">
          <p className="text-sm font-bold text-gray-400 uppercase tracking-wide">Estado</p>
          <p className="text-crealab-text font-semibold">
            Sesión activa. Dashboard en construcción — próximamente verás tus módulos y progreso.
          </p>
        </div>

        <a
          href="/"
          className="text-sm text-brand-orange font-bold hover:underline self-start"
        >
          ← Cerrar sesión
        </a>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Crear directorio app/curso/[id]**

```bash
mkdir -p /Users/federicovera/Documents/Software/crealab/frontend/app/curso/\[id\]
```

- [ ] **Step 3: Crear frontend/app/curso/[id]/page.tsx**

```typescript
interface Props {
  params: { id: string };
}

export default function CursoPage({ params }: Props) {
  return (
    <main className="min-h-screen bg-crealab-bg flex flex-col items-center justify-center p-8">
      <div className="max-w-xl w-full flex flex-col gap-6">
        <div className="flex flex-col gap-1">
          <h1 className="text-4xl font-black text-crealab-text">
            Mi Curso 💻
          </h1>
          <p className="text-gray-500 font-semibold">
            Curso #{params.id}
          </p>
        </div>

        <div className="bg-white rounded-2xl p-6 shadow-sm border border-gray-100 flex flex-col gap-2">
          <p className="text-sm font-bold text-gray-400 uppercase tracking-wide">Estado</p>
          <p className="text-crealab-text font-semibold">
            El contenido del curso se cargará aquí próximamente.
          </p>
        </div>

        <a
          href="/dashboard"
          className="text-sm text-brand-orange font-bold hover:underline self-start"
        >
          ← Volver al dashboard
        </a>
      </div>
    </main>
  );
}
```

- [ ] **Step 4: Commit**

```bash
cd /Users/federicovera/Documents/Software/crealab
git add frontend/app/dashboard/page.tsx frontend/app/curso
git commit -m "feat: páginas placeholder /dashboard y /curso/[id] protegidas por middleware"
```

---

## Task 7: Deploy y verificar en producción

**Files:** ninguno nuevo

- [ ] **Step 1: Push a GitHub**

```bash
cd /Users/federicovera/Documents/Software/crealab
git push
```

- [ ] **Step 2: En el servidor, actualizar .env con JWT_SECRET real**

```bash
cd /opt/crealab
# Generar secreto aleatorio
openssl rand -hex 48
# Editar .env y agregar JWT_SECRET=<valor generado>
nano .env
```

- [ ] **Step 3: Reiniciar WordPress y frontend**

```bash
cd /opt/crealab
docker compose up -d --build crealab_wp crealab_frontend
```

- [ ] **Step 4: Verificar que el endpoint JWT funciona**

```bash
curl -s https://crealab.flowhub.com.ar/wp-json/jwt-auth/v1/token \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"username":"EMAIL_DE_USUARIO","password":"CONTRASENA"}' | python3 -m json.tool
```

Expected: `{"token":"eyJ...","user_email":"...","user_nicename":"...","user_display_name":"..."}`

- [ ] **Step 5: Verificar el flujo completo en producción**

1. Abrir `https://app.crealab.flowhub.com.ar`
2. Ingresar con usuario de WordPress real
3. Verificar redirect a `/dashboard` (o `/curso/[id]` si tiene curso activo)
4. Verificar que `/dashboard` directo sin login redirige a `/`
5. Verificar que aparece "Tu sesión expiró" si se accede protegido sin cookie
